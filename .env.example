# Base URL for the (future) backend evidence API.
# The current build ships with an in-memory mock service, so this is optional.
VITE_API_BASE_URL=https://api.ayush-evidence.gov.in
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id
